services:
  - type: web
    name: notewell-ai-api
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OPENCLAW_TOKEN
        sync: false
      - key: OPENCLAW_GATEWAY_URL
        sync: false
    healthCheckPath: /api/health
    autoDeploy: true